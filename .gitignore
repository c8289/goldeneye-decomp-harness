# ---- generated by dcm.py ----
# All of these are written by `pull` and `build`. They are outputs, not source.

meta.json
src.c
best.c
ctx.h
target.txt
last.json
lastfail.json
LOG.md

# forensic copies kept when the harness halts on a failure or a regression
fail_iter*.c
regress_iter*.c

# sibling sources fetched via `dcm.py family --get <slug>`
sib_*.c

# per-scratch working directories
work/

# ---- editor / OS noise ----
__pycache__/
*.pyc
*.bak
.vscode/
.idea/
.DS_Store
Thumbs.db
